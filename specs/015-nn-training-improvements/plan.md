# Implementation Plan: NN Training Improvements

**Branch**: `015-nn-training-improvements` | **Date**: 2026-03-16 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `specs/015-nn-training-improvements/spec.md`

## Summary

Replace the single-scalar fitness aggregation (49 scenarios × 600 timesteps → 1 double) with
structured per-scenario × per-component scoring. Workers return decomposed score vectors; autoc
applies configurable multi-objective selection strategies. Add control smoothness as explicit
objective to eliminate bang-bang. Per-segment temporal credit rewards error reduction on hard
maneuvers. Research spikes explore lexicase selection and CMA-ES on branches.

## Technical Context

**Language/Version**: C++17
**Primary Dependencies**: Eigen, cereal (serialization), inih (config), GoogleTest
**Storage**: Binary files (NN01 format), S3 archives
**Testing**: GoogleTest via CTest (`bash scripts/rebuild.sh` builds and runs)
**Target Platform**: Linux (aarch64 training, x86 cross-verified), ARM Cortex-M4 (xiao deploy)
**Project Type**: CLI evolution engine + simulation workers
**Performance Goals**: 49 scenarios × pop_size evaluations per generation, parallel across EvalThreads workers
**Constraints**: POSIX sockets between autoc↔workers, cereal binary serialization, deterministic simulation
**Scale/Scope**: 531-weight NN, pop=5000 (GA) or ~50 (CMA-ES), 49 scenarios, 600 timesteps each

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Testing-First | PASS | Contract tests before implementation. Research spikes exempt per constitution. |
| II. Build Stability | PASS | All changes must pass `bash scripts/rebuild.sh`. RPC changes update both sides simultaneously. |
| III. No Compatibility Shims | PASS | No backward compat required (clarification Q1). Old fitness code replaced, not wrapped. |
| IV. Unified Build | PASS | All new code in existing CMake structure. No new build targets needed. |

## Project Structure

### Documentation (this feature)

```text
specs/015-nn-training-improvements/
├── plan.md              # This file
├── research.md          # Fitness analysis, selection strategies, archived findings
├── data-model.md        # ScenarioScore, SegmentScore, FitnessAggregator
├── quickstart.md        # Build and run instructions
├── contracts/
│   ├── structured-fitness.md  # EvalResults wire format change
│   └── config.md              # New INI keys
├── spec.md              # Feature specification
└── tasks.md             # Implementation tasks (generated by /speckit.tasks)
```

### Source Code (changes to existing files)

```text
include/autoc/
├── rpc/protocol.h           # MODIFY: add ScenarioScore, SegmentScore to EvalResults
├── autoc.h                  # MODIFY: fitness constants stay, aggregation moves to new module
├── eval/
│   ├── fitness_aggregator.h  # NEW: AggregationMode, aggregate() function
│   └── segment_scorer.h      # NEW: SegmentBoundary, computeSegmentScores()
└── nn/
    └── population.h          # MODIFY: add per-scenario fitness storage to NNGenome

src/
├── autoc.cc                 # MODIFY: replace computeNNFitness() call with aggregator
├── eval/
│   ├── fitness_aggregator.cc # NEW: sum/minimax/percentile aggregation
│   └── segment_scorer.cc     # NEW: segment boundary detection + scoring
├── nn/
│   └── population.cc         # MODIFY: sigma floor clamping in mutation
└── util/
    └── config.cc             # MODIFY: parse new config keys

tests/
├── sigma_floor_tests.cc      # NEW
├── fitness_aggregator_tests.cc # NEW
├── structured_fitness_tests.cc # NEW: ScenarioScore serialization
└── segment_scorer_tests.cc    # NEW

# Worker side (minisim + crrcsim)
src/minisim.cc               # MODIFY: compute ScenarioScore instead of raw states
crrcsim/src/mod_inputdev/...  # MODIFY: same structured response
```

**Structure Decision**: All new code fits existing directory structure. Two new files in
`eval/` (fitness_aggregator, segment_scorer). No new build targets or subdirectories.

## Implementation Phases

### Phase 1: Sigma Floor (US1) — quick unblock

1. Add `NNSigmaFloor` to config parser
2. Clamp sigma in `nn_gaussian_mutation()` after self-adaptive update
3. Warn if floor > initial sigma
4. Tests: sigma never drops below floor, floor=0 matches unclamped

**Estimated scope**: ~50 LOC change, ~50 LOC tests

### Phase 2: Structured Fitness Return (US9) — infrastructure

1. Define `ScenarioScore` and `SegmentScore` structs in protocol.h
2. Add `scenario_scores` field to `EvalResults`
3. Move fitness component computation from `computeNNFitness()` to worker side
4. Worker computes distance_rmse, attitude_error per scenario
5. Autoc receives structured scores, computes aggregate for backward-compat logging
6. Tests: round-trip serialization, aggregate from components matches legacy scalar

**Key risk**: Ensuring the decomposed scores recombine to match the existing scalar exactly.
Write a comparison test before splitting the computation.

### Phase 3: Smoothness Objective (US10) — kills bang-bang

1. Add smoothness computation to worker evaluation loop: `|u(t) - u(t-1)|` per axis
2. Include in ScenarioScore response
3. Log smoothness per generation alongside fitness
4. Tests: bang-bang input → smoothness ≈ 2.0, constant input → 0.0

**Depends on**: Phase 2 (structured response format)

### Phase 4: Multi-Objective Selection (US3) — core hypothesis test

1. Implement `FitnessAggregator` with sum/minimax/percentile modes
2. Add `FitnessAggregation` config key
3. Wire into evolution loop: replace `computeNNFitness()` call with aggregator
4. Add per-scenario fitness storage to NNGenome for logging/analysis
5. Tests: minimax on known vectors, percentile computation, sum matches legacy

**Depends on**: Phase 2 (structured scores to aggregate)

### Phase 5: Per-Segment Temporal Credit (US11)

1. Compute segment boundaries from path geometry in worker
2. Score segments by error reduction × difficulty weight
3. Include in ScenarioScore response
4. Wire into aggregator as optional weighting
5. Tests: known trajectory segments produce expected scores

**Depends on**: Phase 2 (structured response), path geometry available in worker

### Phase 6: Research Spikes (US12) — branched experiments

Each on its own branch, validated against Phase 4 baseline:

- **Lexicase selection**: Per-scenario test cases, no aggregation
- **NSGA-II**: Pareto ranking on (tracking, smoothness, robustness)
- **MAP-Elites**: Behavioral grid (mean tracking × smoothness)
- **sep-CMA-ES** (US4): Ask/tell optimizer, pop=50

### Phase 7: Polish

1. Update autoc.ini with all new keys and sensible defaults
2. Verify all builds: autoc, crrcsim, xiao
3. Run full test suite
4. Verify export pipeline: nnextractor → nn2cpp → xiao build

## Complexity Tracking

No constitution violations requiring justification.
